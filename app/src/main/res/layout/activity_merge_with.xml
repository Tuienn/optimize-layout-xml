<?xml version="1.0" encoding="utf-8"?>
<!-- WITH <merge> tag demonstration -->
<!-- Hierarchy depth: 1 level (Flat - views attached directly to root) -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:gravity="center">

        <!-- Card Container -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="#FFFFFF"
            android:elevation="4dp"
            android:padding="16dp"
            android:layout_marginTop="24dp">

            <!-- Form Title -->
            <TextView
                android:id="@+id/tvFormTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/merge_form_title"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="#212121"
                android:layout_gravity="center_horizontal"
                android:layout_marginBottom="24dp" />

            <!-- Include Field 1 - Views attached directly (no wrapper) -->
            <include layout="@layout/reuse_layout_with_merge" />

            <!-- Include Field 2 - Views attached directly (no wrapper) -->
            <include layout="@layout/reuse_layout_with_merge" />

            <!-- Include Field 3 - Views attached directly (no wrapper) -->
            <include layout="@layout/reuse_layout_with_merge" />

            <!-- Info Text -->
            <TextView
                android:id="@+id/tvHierarchyInfo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/with_merge_info"
                android:textSize="12sp"
                android:textColor="#4CAF50"
                android:background="#E8F5E9"
                android:padding="12dp"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="16dp" />

            <!-- Submit Button -->
            <Button
                android:id="@+id/btnSubmit"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/btn_submit"
                android:textSize="16sp"
                android:textColor="#FFFFFF"
                android:background="@drawable/bg_button_secondary"
                android:padding="16dp" />

        </LinearLayout>

    </LinearLayout>

</ScrollView>
